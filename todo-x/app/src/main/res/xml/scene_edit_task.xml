<?xml version="1.0" encoding="utf-8"?>
<MotionScene xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <Transition
        app:constraintSetEnd="@+id/setCollapsed"
        app:constraintSetStart="@+id/setExpanded">

        <OnSwipe
            app:dragDirection="dragUp"
            app:touchAnchorId="@+id/content"
            app:touchAnchorSide="top" />

    </Transition>

    <ConstraintSet android:id="@+id/setExpanded">

        <Constraint
            android:id="@+id/toolbar"
            android:layout_width="0dp"
            android:layout_height="@dimen/edit_toolbar_expanded" />

        <Constraint
            android:id="@+id/txvTitle"
            android:layout_width="0dp"
            android:layout_height="@dimen/edit_title_height_expanded"
            app:layout_constraintEnd_toEndOf="@id/toolbar"
            app:layout_constraintBottom_toBottomOf="@id/toolbar"
            android:layout_marginBottom="@dimen/edit_title_margin_bottom_expended"
            android:layout_marginStart="@dimen/edit_title_margin_start_expended"
            app:layout_constraintStart_toStartOf="@id/toolbar">
            <CustomAttribute
                app:attributeName="textSize"
                app:customDimension="@dimen/edit_title_text_size_expanded" />
        </Constraint>

        <Constraint android:id="@+id/progressbar">
            <PropertySet
                app:applyMotionScene="false"
                app:visibilityMode="ignore" />
        </Constraint>

    </ConstraintSet>


    <ConstraintSet android:id="@+id/setCollapsed">

        <Constraint
            android:id="@+id/toolbar"
            android:layout_width="0dp"
            android:layout_height="@dimen/edit_toolbar_collapsed" />

        <Constraint
            android:id="@+id/txvTitle"
            android:layout_width="0dp"
            android:layout_height="@dimen/edit_title_height_collapsed"
            app:layout_constraintEnd_toEndOf="@id/toolbar"
            app:layout_constraintBottom_toBottomOf="@id/btnBack"
            app:layout_constraintStart_toEndOf="@id/btnBack"
            app:layout_constraintTop_toTopOf="@id/btnBack">
            <CustomAttribute
                app:attributeName="textSize"
                app:customDimension="@dimen/edit_title_text_size_collapsed" />
        </Constraint>

        <Constraint android:id="@+id/progressbar">
            <PropertySet
                app:applyMotionScene="false"
                app:visibilityMode="ignore" />
        </Constraint>

    </ConstraintSet>

</MotionScene>